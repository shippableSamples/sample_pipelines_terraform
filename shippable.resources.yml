resources:

################################

# add integration for AWS
  - name: integration-aws
    type: integration
    integration: demo-manishas-ecs

# add gitRepo for Terraform scripts
  - name: repo-tfScripts
    type: gitRepo
    integration: github-manishas
    pointer:
      sourceName: manishas/sample_pipelines_terraform
      branch: master

  # env vars for Terraform scripts
  - name: params-tfScripts
    type: params
    version:
      params:
        REGION: "us-east-1"
